
CONCATENATE(CLIENT_NEED_ACCESS)
LOAD DISTINCT
    [Need Id] AS %FACT_NEED_KEY_SEC,
	[TIER_LINK] & '|' & [Client Id] AS %SA_CLIENT_TEST
RESIDENT NEED_ACCESS_TIER1;

DROP TABLE NEED_ACCESS_TIER1;


STORE CLIENT_NEED_ACCESS INTO [lib://$(vConnection)/$(vQVDStoreType)/$(vDBname)/$(vStoreQVDTypePrefix)_CLIENT_NEED_ACCESS.qvd];
DROP TABLE CLIENT_NEED_ACCESS;
	